{
  "hosting": {
    "public": ".",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ]
    // The "rewrites" section should be removed or commented out for now
    // If you plan to use rewrites for other purposes later (like SPAs),
    // we can re-add them, but they shouldn't force /intake.html as the default.
  }
}
